import React from 'react'
import { useParams } from 'react-router-dom'

const MatchStats = (props) => {

    //let { slug } = useParams()

    return (
        <div>
            This is the match stats page for match
        </div>
    )
}

export default MatchStats
